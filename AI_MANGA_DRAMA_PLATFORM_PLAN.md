# AI 漫剧平台 MVP 创作闭环计划

## 1. 项目定位

本项目要建设一个 Web 全栈 AI 漫剧创作平台。第一版目标不是做一个泛内容工具，而是围绕“输入剧本后自动生成一集漫剧草稿”的创作闭环，帮助创作者把剧本快速拆解为人物、场景、道具、分镜、画面、配音、字幕和视频。

平台第一版的核心价值是：

- 用户输入完整剧本、故事梗概或分场脚本。
- 系统自动拆解人物、场景、道具、工具、关键物品和分镜。
- 系统生成统一的设定库，保证整部漫剧的人物、服装、道具、工具和场景风格一致。
- 系统基于设定库生成镜头画面、台词配音、字幕和视频草稿。
- 用户可以编辑设定、分镜和生成结果，再导出 MP4 成片。

第一版优先解决“从剧本到一集漫剧草稿”的可用闭环。支付、团队协作、模板市场、公开社区和多平台发布管理放到后续阶段。

## 2. MVP 范围

### 2.1 第一版必须包含

- 项目创建和剧本输入。
- 剧本解析和结构化拆解。
- 人物设定卡自动生成。
- 道具/工具/关键物品设定卡自动生成。
- 场景和世界观设定卡自动生成。
- 分镜自动生成。
- 人物、道具、场景设定编辑和锁定。
- 基于设定卡的一致性提示词生成。
- 镜头图像生成任务。
- 台词、旁白、字幕文本生成。
- 配音任务和音频资产管理。
- 视频合成任务和 MP4 导出。
- 任务状态、失败重试和基础错误提示。

### 2.2 第一版暂不包含

- 复杂团队协作和权限体系。
- 订阅、支付、发票和商业化后台。
- 模型训练、角色 LoRA 训练和专属模型微调。
- 模板市场、社区发布和评论系统。
- 多语言翻译和多平台自动分发。
- 高级剪辑器能力，例如逐帧动画编辑、复杂特效和专业音轨混音。

## 3. 用户创作流程

### 3.1 创建项目

用户进入平台后创建一个漫剧项目，并填写：

- 项目名称。
- 漫剧类型，例如都市、玄幻、悬疑、恋爱、科幻、校园。
- 目标比例，例如 9:16 竖屏、16:9 横屏、1:1 方屏。
- 默认美术风格，例如国漫、日漫、厚涂、赛博、写实漫画。
- 目标时长，例如 1 分钟、3 分钟、5 分钟。

### 3.2 输入剧本

用户可以输入三类内容：

- 完整剧本：包含场景、人物、台词、动作。
- 故事梗概：系统自动补全为短剧脚本。
- 分场脚本：系统按已有分场生成分镜。

剧本输入后，系统先保存原文，再进入 AI 解析流程。

### 3.3 AI 剧本解析

系统自动识别并结构化输出：

- 故事标题。
- 剧情简介。
- 集数和当前集信息。
- 场景列表。
- 人物列表。
- 人物关系。
- 道具、工具、武器、设备、载具和关键物品。
- 服装、发型、标志性特征。
- 台词风格。
- 重要动作和情绪变化。
- 可拆分的分镜节点。

### 3.4 生成设定总表

系统把解析结果转化为项目级设定库，包括：

- 人物设定卡。
- 道具和工具设定卡。
- 场景设定卡。
- 世界观设定。
- 项目美术风格。
- 一致性提示词规则。

用户必须先确认或修改设定总表，再进入大规模分镜和素材生成。这样可以减少后续生成时人物变脸、服装混乱、道具变形和场景风格漂移。

### 3.5 自动生成分镜

系统按剧本拆解镜头，每个镜头包含：

- 镜头编号。
- 所属场景。
- 出场人物。
- 出现道具。
- 镜头景别。
- 镜头角度。
- 动作描述。
- 情绪描述。
- 台词或旁白。
- 字幕文本。
- 图像生成提示词。
- 负面提示词。
- 配音文本。
- 预计时长。

### 3.6 生成资产和视频

每个镜头都引用已锁定的人物、道具和场景设定。生成流程为：

1. 生成或选择角色参考图。
2. 生成或选择道具参考图。
3. 生成镜头画面。
4. 生成配音音频。
5. 生成字幕。
6. 合成镜头片段。
7. 合成整集视频。
8. 导出 MP4。

## 4. 一致性系统设计

一致性系统是平台的核心能力。所有生成任务都不能只依赖自由文本提示词，必须绑定项目设定库。

### 4.1 人物一致性

每个人物生成唯一 `character_id`，并维护人物设定卡：

- 姓名。
- 别名。
- 角色类型：主角、配角、反派、路人。
- 年龄段。
- 性别。
- 身高体型。
- 脸型、五官、肤色。
- 发型和发色。
- 常用服装。
- 标志性配饰。
- 性格。
- 说话风格。
- 声音设定。
- 人物关系。
- 参考图。
- 正向提示词。
- 负面提示词。
- 锁定状态。
- AI 补全标记。

一致性规则：

- 分镜生成时只引用 `character_id`，不重新发明人物外观。
- 图像生成提示词必须拼接人物设定卡中的固定描述。
- 如果人物设定卡被用户锁定，AI 后续不得自动改写核心外观。
- 如果用户修改人物设定，新镜头使用新版设定。
- 已生成镜头可以选择保持不变或批量重生成。

### 4.2 道具和工具一致性

道具、工具、武器、设备、载具和关键物品统一归入 `PropBible`。每个关键物品生成唯一 `prop_id`：

- 名称。
- 类型：道具、工具、武器、设备、载具、关键物品。
- 用途。
- 所属人物或所属场景。
- 材质。
- 主色。
- 形状。
- 尺寸。
- 纹理和装饰。
- 使用方式。
- 首次出现镜头。
- 参考图。
- 正向提示词。
- 负面提示词。
- 锁定状态。
- AI 补全标记。

一致性规则：

- 同一 `prop_id` 在所有镜头中保持颜色、形状、用途和持有人一致。
- 重要道具生成参考图后，后续镜头必须引用参考图或固定提示词。
- 系统发现剧本中出现相似道具名称时，应提示用户合并或拆分。

### 4.3 场景一致性

每个重要地点生成唯一 `scene_id`：

- 场景名称。
- 场景类型：室内、室外、城市、校园、战场、梦境、虚拟空间等。
- 时代背景。
- 空间布局。
- 主色调。
- 光线。
- 天气。
- 关键物件。
- 氛围。
- 参考图。
- 正向提示词。
- 负面提示词。
- 锁定状态。
- AI 补全标记。

一致性规则：

- 同一场景在多个镜头中保持空间布局、色调和关键物件一致。
- 同一场景可以有时间变体，例如白天、夜晚、雨天，但变体必须挂在同一个 `scene_id` 下。

### 4.4 一致性检查

视频导出前执行一致性检查：

- 是否有镜头引用了不存在的人物。
- 是否有镜头引用了不存在的道具。
- 同一人物是否出现明显服装或外貌冲突。
- 同一道具是否出现明显颜色、形状或持有人冲突。
- 场景风格是否偏离项目风格。
- 台词是否遗漏。
- 配音是否缺失。
- 字幕是否缺失。
- 镜头时长是否异常。

检查结果展示为问题列表，用户可以选择忽略、修复或批量重生成。

## 5. 核心模块架构

### 5.1 前端模块

- 项目首页：项目列表、创建项目、最近编辑。
- 剧本输入页：粘贴剧本、上传文本、解析状态。
- 设定总表页：人物库、道具库、场景库、世界观设定。
- 分镜工作台：镜头列表、时间线、分镜卡片、镜头预览。
- 资产库：图片、角色参考图、道具参考图、场景参考图、音频、视频片段。
- 生成任务中心：任务队列、状态、失败原因、重试。
- 视频预览页：整集预览、字幕、音轨、导出。

### 5.2 后端模块

- Project Service：项目和基础配置管理。
- Script Service：剧本保存、版本管理和解析。
- Bible Service：人物、道具、场景、世界观设定管理。
- Storyboard Service：分镜生成和编辑。
- Prompt Service：一致性提示词拼接和版本管理。
- Generation Service：AI 生成任务调度。
- Asset Service：素材上传、存储、索引和引用。
- Voice Service：配音任务和音频资产。
- Render Service：视频合成和导出。
- Task Queue：异步任务队列、状态更新、失败重试。

### 5.3 存储模块

- PostgreSQL：保存项目、剧本、设定卡、分镜、任务和资产元数据。
- S3 兼容对象存储：保存图片、音频、视频、参考图和导出文件。
- Redis：保存队列状态、临时任务状态和限流信息。

## 6. 推荐技术栈

### 6.1 Web 应用

- 前端框架：Next.js + React。
- UI 组件：Tailwind CSS + shadcn/ui 或同类组件库。
- 后端 API：Next.js Route Handlers 或独立 Node.js 服务。
- 类型系统：TypeScript。

### 6.2 数据和任务

- 数据库：PostgreSQL。
- ORM：Prisma 或 Drizzle。
- 队列：BullMQ + Redis。
- 文件存储：S3、Cloudflare R2、MinIO 或其他 S3 兼容服务。

### 6.3 AI 能力

- 大语言模型：用于剧本解析、设定卡生成、分镜生成、提示词生成和一致性检查。
- 图像生成模型：用于角色参考图、道具参考图、场景图和镜头画面。
- 语音合成模型：用于台词和旁白配音。
- 视频合成：第一版可用 FFmpeg 把图片、音频、字幕和转场合成为 MP4。

## 7. 数据模型草案

### 7.1 Project

- `id`
- `name`
- `genre`
- `aspect_ratio`
- `visual_style`
- `target_duration`
- `status`
- `created_at`
- `updated_at`

### 7.2 Script

- `id`
- `project_id`
- `raw_text`
- `normalized_text`
- `script_type`
- `analysis_status`
- `analysis_result`
- `version`
- `created_at`
- `updated_at`

### 7.3 CharacterBible

- `id`
- `project_id`
- `character_id`
- `name`
- `role_type`
- `appearance`
- `hair`
- `costume`
- `personality`
- `voice_profile`
- `relationships`
- `reference_asset_id`
- `positive_prompt`
- `negative_prompt`
- `is_locked`
- `ai_completed_fields`
- `created_at`
- `updated_at`

### 7.4 PropBible

- `id`
- `project_id`
- `prop_id`
- `name`
- `prop_type`
- `purpose`
- `owner_character_id`
- `scene_id`
- `material`
- `color`
- `shape`
- `size`
- `reference_asset_id`
- `positive_prompt`
- `negative_prompt`
- `is_locked`
- `ai_completed_fields`
- `created_at`
- `updated_at`

### 7.5 WorldBible

- `id`
- `project_id`
- `scene_id`
- `name`
- `scene_type`
- `era`
- `layout`
- `color_palette`
- `lighting`
- `weather`
- `mood`
- `key_objects`
- `reference_asset_id`
- `positive_prompt`
- `negative_prompt`
- `is_locked`
- `created_at`
- `updated_at`

### 7.6 StoryboardShot

- `id`
- `project_id`
- `episode_id`
- `shot_number`
- `scene_id`
- `character_ids`
- `prop_ids`
- `camera_size`
- `camera_angle`
- `action`
- `emotion`
- `dialogue`
- `subtitle`
- `voice_text`
- `duration_seconds`
- `image_prompt`
- `negative_prompt`
- `image_asset_id`
- `audio_asset_id`
- `video_asset_id`
- `status`
- `created_at`
- `updated_at`

### 7.7 ConsistencyPrompt

- `id`
- `project_id`
- `target_type`
- `target_id`
- `prompt_text`
- `negative_prompt`
- `version`
- `created_at`
- `updated_at`

### 7.8 GeneratedAsset

- `id`
- `project_id`
- `asset_type`
- `storage_url`
- `thumbnail_url`
- `source_task_id`
- `metadata`
- `created_at`

### 7.9 RenderTask

- `id`
- `project_id`
- `task_type`
- `status`
- `input_payload`
- `output_payload`
- `error_message`
- `retry_count`
- `created_at`
- `updated_at`

### 7.10 ExportJob

- `id`
- `project_id`
- `status`
- `format`
- `resolution`
- `output_asset_id`
- `error_message`
- `created_at`
- `updated_at`

## 8. API 草案

### 8.1 项目 API

- `POST /api/projects`：创建项目。
- `GET /api/projects`：获取项目列表。
- `GET /api/projects/:projectId`：获取项目详情。
- `PATCH /api/projects/:projectId`：更新项目配置。

### 8.2 剧本 API

- `POST /api/projects/:projectId/script`：保存剧本。
- `POST /api/projects/:projectId/script/analyze`：提交剧本解析任务。
- `GET /api/projects/:projectId/script/analysis`：获取解析结果。

### 8.3 设定库 API

- `GET /api/projects/:projectId/bible`：获取设定总表。
- `PATCH /api/projects/:projectId/characters/:characterId`：更新人物设定卡。
- `PATCH /api/projects/:projectId/props/:propId`：更新道具设定卡。
- `PATCH /api/projects/:projectId/scenes/:sceneId`：更新场景设定卡。
- `POST /api/projects/:projectId/bible/lock`：锁定设定库。

### 8.4 分镜 API

- `POST /api/projects/:projectId/storyboard/generate`：生成分镜。
- `GET /api/projects/:projectId/storyboard`：获取分镜列表。
- `PATCH /api/projects/:projectId/storyboard/:shotId`：更新单个镜头。
- `POST /api/projects/:projectId/storyboard/:shotId/regenerate`：重生成单个镜头。

### 8.5 生成任务 API

- `POST /api/projects/:projectId/generate/reference`：生成角色、道具或场景参考图。
- `POST /api/projects/:projectId/generate/images`：生成镜头图片。
- `POST /api/projects/:projectId/generate/voices`：生成配音。
- `POST /api/projects/:projectId/render`：提交视频合成任务。
- `GET /api/tasks/:taskId`：查询任务状态。
- `POST /api/tasks/:taskId/retry`：重试失败任务。

### 8.6 导出 API

- `POST /api/projects/:projectId/consistency-check`：执行一致性检查。
- `POST /api/projects/:projectId/export`：导出 MP4。
- `GET /api/exports/:exportJobId`：查询导出状态。
- `GET /api/assets/:assetId/download`：下载导出文件。

## 9. AI 工作流

### 9.1 剧本解析 Prompt 目标

输入：剧本文本、项目类型、目标风格。

输出：

- 结构化剧情。
- 人物候选列表。
- 道具候选列表。
- 场景候选列表。
- 情节节点。
- 分镜建议。
- 信息缺失项。

要求：

- 所有 AI 补全字段必须标记。
- 人物重名或道具歧义必须返回警告。
- 不确定的设定不能直接覆盖用户已有设定。

### 9.2 设定卡生成 Prompt 目标

输入：剧本解析结果、项目风格、已有设定。

输出：

- 标准人物设定卡。
- 标准道具设定卡。
- 标准场景设定卡。
- 正向提示词。
- 负面提示词。

要求：

- 每张设定卡有唯一 ID。
- 外观、服装、材质、颜色等字段尽量具体。
- 不允许同一角色在不同字段中出现冲突描述。

### 9.3 分镜生成 Prompt 目标

输入：剧本、设定卡、项目风格。

输出：

- 镜头列表。
- 镜头和人物、道具、场景的绑定关系。
- 图像提示词。
- 配音文本。
- 字幕文本。
- 镜头时长。

要求：

- 镜头中的人物只能引用已有 `character_id`。
- 镜头中的道具只能引用已有 `prop_id`。
- 场景只能引用已有 `scene_id`，必要时生成待确认的新场景。
- 图像提示词必须使用设定卡拼接，而不是重新描述人物和道具。

### 9.4 一致性检查 Prompt 目标

输入：设定库、分镜、已生成资产元数据。

输出：

- 问题列表。
- 问题等级：阻塞、警告、建议。
- 建议修复动作。

检查项：

- 人物引用缺失。
- 道具引用缺失。
- 场景引用缺失。
- 人物外观冲突。
- 道具外观冲突。
- 台词和字幕缺失。
- 镜头顺序异常。

## 10. 开发阶段

### 阶段一：规划文档和产品原型

目标：

- 完成本文档。
- 明确核心页面和用户流程。
- 设计数据库模型和 API 边界。

验收：

- 项目根目录存在完整 Markdown 计划。
- 能根据计划拆出开发任务。

### 阶段二：项目和剧本解析

目标：

- 实现项目创建。
- 实现剧本输入。
- 实现剧本解析任务。
- 输出人物、道具、场景候选。

验收：

- 输入一段剧本后，可以得到结构化解析结果。
- 解析结果能保存并展示。

### 阶段三：设定库和一致性提示词

目标：

- 实现人物设定卡。
- 实现道具设定卡。
- 实现场景设定卡。
- 实现设定卡编辑和锁定。
- 实现一致性提示词拼接。

验收：

- 同一人物、道具、场景有固定 ID 和设定。
- 用户修改并锁定设定后，后续生成使用锁定内容。

### 阶段四：分镜工作台

目标：

- 根据剧本和设定库生成分镜。
- 分镜绑定人物、道具和场景。
- 支持编辑镜头台词、动作、提示词和时长。

验收：

- 可以从剧本生成镜头列表。
- 每个镜头可以看到引用的人物、道具和场景。

### 阶段五：图像、配音和视频生成

目标：

- 生成角色和道具参考图。
- 生成镜头图像。
- 生成配音和字幕。
- 合成整集视频。
- 导出 MP4。

验收：

- 用户能从一个项目导出一支可播放 MP4。
- 失败任务可重试。

### 阶段六：一致性检查和质量提升

目标：

- 导出前执行一致性检查。
- 支持发现缺失引用和明显冲突。
- 支持批量重生成有问题的镜头。

验收：

- 导出前可以看到问题列表。
- 用户可以修复或忽略问题。

## 11. 测试计划

### 11.1 剧本解析测试

- 输入完整剧本，验证系统能抽取主要人物、配角、道具、工具和场景。
- 输入故事梗概，验证系统能补全人物和基础分场。
- 输入分场脚本，验证系统能保持原分场结构。
- 输入超长剧本，验证系统能分段解析并合并结果。
- 输入人物重名剧本，验证系统能提示用户确认。

### 11.2 一致性测试

- 验证同一人物在多个镜头中的姓名、外貌、服装、声音和参考图保持一致。
- 验证同一道具在多个镜头中的颜色、形状、用途和持有人保持一致。
- 验证同一场景在多个镜头中的布局、色调和关键物件保持一致。
- 验证用户修改人物或道具设定后，新生成镜头引用最新设定。
- 验证已锁定设定不会被 AI 自动覆盖。

### 11.3 分镜测试

- 验证每个镜头都有镜头编号、场景、人物、动作、台词和时长。
- 验证镜头引用的人物、道具和场景 ID 存在。
- 验证缺失字段会被标记为 AI 补全或待确认。

### 11.4 生成任务测试

- 验证图像生成任务状态流转：等待中、生成中、成功、失败、可重试。
- 验证配音任务失败后可以重试。
- 验证视频合成失败不会丢失项目数据。
- 验证素材缺失时导出被阻止并显示原因。

### 11.5 导出测试

- 验证完整项目可以导出 MP4。
- 验证导出视频包含画面、配音和字幕。
- 验证导出任务状态可查询。
- 验证导出文件可下载。

## 12. 风险和应对

### 12.1 人物一致性风险

风险：图像模型可能在不同镜头中改变人物脸型、服装或发型。

应对：

- 使用人物设定卡。
- 使用角色参考图。
- 锁定核心外观提示词。
- 后续阶段支持角色 LoRA 或参考图训练。

### 12.2 道具一致性风险

风险：同一工具或道具在不同镜头中形状和颜色变化。

应对：

- 为关键道具建立 `prop_id`。
- 生成道具参考图。
- 在镜头提示词中强制引用道具设定。
- 导出前执行道具一致性检查。

### 12.3 剧本解析不稳定风险

风险：AI 可能漏掉人物、混淆道具或误解剧情。

应对：

- 剧本解析结果必须先给用户确认。
- AI 补全字段明确标记。
- 人物重名和道具歧义要提示用户。

### 12.4 生成成本风险

风险：图片、配音和视频任务成本较高。

应对：

- 第一版默认先生成低清预览。
- 用户确认后再生成高清版本。
- 长任务进入队列，支持限流和重试。

### 12.5 视频合成复杂度风险

风险：直接做高级视频编辑器会拖慢 MVP。

应对：

- 第一版用图片、音频、字幕和简单转场合成 MP4。
- 高级剪辑能力后续迭代。

## 13. 后续扩展路线

### 13.1 第二阶段

- 角色 LoRA 或参考图训练。
- 多集批量生成。
- 剧集世界观管理。
- 风格模板库。
- 竖屏短剧平台导出预设。

### 13.2 第三阶段

- 团队协作。
- 评论和审阅。
- 订阅和支付。
- 模板市场。
- 生成额度管理。

### 13.3 第四阶段

- 多语言版本。
- 自动翻译和多语配音。
- 多平台发布。
- 数据看板和内容表现分析。

## 14. 第一版验收标准

第一版完成时，应满足以下标准：

- 用户可以创建一个 AI 漫剧项目。
- 用户可以输入剧本并保存。
- 系统可以自动拆解人物、道具、工具、场景和分镜。
- 系统可以生成并展示人物、道具、场景设定卡。
- 用户可以编辑并锁定设定卡。
- 系统可以基于设定卡生成分镜。
- 每个镜头可以引用固定人物、道具和场景。
- 系统可以生成镜头画面、配音和字幕。
- 系统可以合成并导出 MP4。
- 导出前可以执行一致性检查。
- 失败任务可以看到原因并重试。

## 15. 默认决策

- 第一版以剧本输入为核心，不优先做纯主题一键成片。
- “工具”按漫剧生产中的道具、武器、设备、载具和关键物品统一处理。
- 第一版追求人物和道具一致性，但不强依赖模型微调。
- 一致性优先通过设定卡、参考图、统一提示词和导出前检查实现。
- 第一版先做可用闭环，再逐步增强画面质量、角色稳定性和商业化能力。
